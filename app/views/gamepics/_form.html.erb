<%= form_with(model: gamepic, local: true) do |form| %>
  <% if gamepic.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(gamepic.errors.count, "error") %> prohibited this gamepic from being saved:</h2>

      <ul>
      <% gamepic.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :url %>
    <%= form.text_field :url %>
  </div>

  <div class="field">
    <%= form.label :game_id %>
    <%= form.text_field :game_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
