<%= form_with(model: partspic, local: true) do |form| %>
  <% if partspic.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(partspic.errors.count, "error") %> prohibited this partspic from being saved:</h2>

      <ul>
      <% partspic.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :url %>
    <%= form.text_field :url %>
  </div>

  <div class="field">
    <%= form.label :game_id %>
    <%= form.text_field :game_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
